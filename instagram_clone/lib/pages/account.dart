import 'package:flutter/material.dart';

class UseAccount extends StatelessWidget {
  const UseAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('ACCOUNT')),
    );
  }
}
